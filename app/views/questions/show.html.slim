= render partial: 'questions/question', locals: { question: @question, source_view: @source_view }

div id="answers"
  = render partial: 'answers/answer', collection: @question.answers

- if user_signed_in?
  = render partial: 'answers/shared/new_answer'
